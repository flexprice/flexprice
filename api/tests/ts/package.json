{
  "name": "flexprice-sdk-tests",
  "private": true,
  "description": "Published SDK integration tests (flexprice-ts-temp)",
  "scripts": {
    "test": "npx ts-node test_sdk_js.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "flexprice-ts-temp": "latest"
  }
}
