// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/predicate"
	"github.com/flexprice/flexprice/ent/stripesyncbatch"
)

// StripeSyncBatchQuery is the builder for querying StripeSyncBatch entities.
type StripeSyncBatchQuery struct {
	config
	ctx        *QueryContext
	order      []stripesyncbatch.OrderOption
	inters     []Interceptor
	predicates []predicate.StripeSyncBatch
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StripeSyncBatchQuery builder.
func (ssbq *StripeSyncBatchQuery) Where(ps ...predicate.StripeSyncBatch) *StripeSyncBatchQuery {
	ssbq.predicates = append(ssbq.predicates, ps...)
	return ssbq
}

// Limit the number of records to be returned by this query.
func (ssbq *StripeSyncBatchQuery) Limit(limit int) *StripeSyncBatchQuery {
	ssbq.ctx.Limit = &limit
	return ssbq
}

// Offset to start from.
func (ssbq *StripeSyncBatchQuery) Offset(offset int) *StripeSyncBatchQuery {
	ssbq.ctx.Offset = &offset
	return ssbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ssbq *StripeSyncBatchQuery) Unique(unique bool) *StripeSyncBatchQuery {
	ssbq.ctx.Unique = &unique
	return ssbq
}

// Order specifies how the records should be ordered.
func (ssbq *StripeSyncBatchQuery) Order(o ...stripesyncbatch.OrderOption) *StripeSyncBatchQuery {
	ssbq.order = append(ssbq.order, o...)
	return ssbq
}

// First returns the first StripeSyncBatch entity from the query.
// Returns a *NotFoundError when no StripeSyncBatch was found.
func (ssbq *StripeSyncBatchQuery) First(ctx context.Context) (*StripeSyncBatch, error) {
	nodes, err := ssbq.Limit(1).All(setContextOp(ctx, ssbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{stripesyncbatch.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) FirstX(ctx context.Context) *StripeSyncBatch {
	node, err := ssbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StripeSyncBatch ID from the query.
// Returns a *NotFoundError when no StripeSyncBatch ID was found.
func (ssbq *StripeSyncBatchQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ssbq.Limit(1).IDs(setContextOp(ctx, ssbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{stripesyncbatch.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) FirstIDX(ctx context.Context) string {
	id, err := ssbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StripeSyncBatch entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StripeSyncBatch entity is found.
// Returns a *NotFoundError when no StripeSyncBatch entities are found.
func (ssbq *StripeSyncBatchQuery) Only(ctx context.Context) (*StripeSyncBatch, error) {
	nodes, err := ssbq.Limit(2).All(setContextOp(ctx, ssbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{stripesyncbatch.Label}
	default:
		return nil, &NotSingularError{stripesyncbatch.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) OnlyX(ctx context.Context) *StripeSyncBatch {
	node, err := ssbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StripeSyncBatch ID in the query.
// Returns a *NotSingularError when more than one StripeSyncBatch ID is found.
// Returns a *NotFoundError when no entities are found.
func (ssbq *StripeSyncBatchQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ssbq.Limit(2).IDs(setContextOp(ctx, ssbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{stripesyncbatch.Label}
	default:
		err = &NotSingularError{stripesyncbatch.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) OnlyIDX(ctx context.Context) string {
	id, err := ssbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StripeSyncBatches.
func (ssbq *StripeSyncBatchQuery) All(ctx context.Context) ([]*StripeSyncBatch, error) {
	ctx = setContextOp(ctx, ssbq.ctx, ent.OpQueryAll)
	if err := ssbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StripeSyncBatch, *StripeSyncBatchQuery]()
	return withInterceptors[[]*StripeSyncBatch](ctx, ssbq, qr, ssbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) AllX(ctx context.Context) []*StripeSyncBatch {
	nodes, err := ssbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StripeSyncBatch IDs.
func (ssbq *StripeSyncBatchQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ssbq.ctx.Unique == nil && ssbq.path != nil {
		ssbq.Unique(true)
	}
	ctx = setContextOp(ctx, ssbq.ctx, ent.OpQueryIDs)
	if err = ssbq.Select(stripesyncbatch.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) IDsX(ctx context.Context) []string {
	ids, err := ssbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ssbq *StripeSyncBatchQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ssbq.ctx, ent.OpQueryCount)
	if err := ssbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ssbq, querierCount[*StripeSyncBatchQuery](), ssbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) CountX(ctx context.Context) int {
	count, err := ssbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ssbq *StripeSyncBatchQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ssbq.ctx, ent.OpQueryExist)
	switch _, err := ssbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ssbq *StripeSyncBatchQuery) ExistX(ctx context.Context) bool {
	exist, err := ssbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StripeSyncBatchQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ssbq *StripeSyncBatchQuery) Clone() *StripeSyncBatchQuery {
	if ssbq == nil {
		return nil
	}
	return &StripeSyncBatchQuery{
		config:     ssbq.config,
		ctx:        ssbq.ctx.Clone(),
		order:      append([]stripesyncbatch.OrderOption{}, ssbq.order...),
		inters:     append([]Interceptor{}, ssbq.inters...),
		predicates: append([]predicate.StripeSyncBatch{}, ssbq.predicates...),
		// clone intermediate query.
		sql:  ssbq.sql.Clone(),
		path: ssbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StripeSyncBatch.Query().
//		GroupBy(stripesyncbatch.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ssbq *StripeSyncBatchQuery) GroupBy(field string, fields ...string) *StripeSyncBatchGroupBy {
	ssbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StripeSyncBatchGroupBy{build: ssbq}
	grbuild.flds = &ssbq.ctx.Fields
	grbuild.label = stripesyncbatch.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.StripeSyncBatch.Query().
//		Select(stripesyncbatch.FieldTenantID).
//		Scan(ctx, &v)
func (ssbq *StripeSyncBatchQuery) Select(fields ...string) *StripeSyncBatchSelect {
	ssbq.ctx.Fields = append(ssbq.ctx.Fields, fields...)
	sbuild := &StripeSyncBatchSelect{StripeSyncBatchQuery: ssbq}
	sbuild.label = stripesyncbatch.Label
	sbuild.flds, sbuild.scan = &ssbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StripeSyncBatchSelect configured with the given aggregations.
func (ssbq *StripeSyncBatchQuery) Aggregate(fns ...AggregateFunc) *StripeSyncBatchSelect {
	return ssbq.Select().Aggregate(fns...)
}

func (ssbq *StripeSyncBatchQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ssbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ssbq); err != nil {
				return err
			}
		}
	}
	for _, f := range ssbq.ctx.Fields {
		if !stripesyncbatch.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ssbq.path != nil {
		prev, err := ssbq.path(ctx)
		if err != nil {
			return err
		}
		ssbq.sql = prev
	}
	return nil
}

func (ssbq *StripeSyncBatchQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StripeSyncBatch, error) {
	var (
		nodes = []*StripeSyncBatch{}
		_spec = ssbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StripeSyncBatch).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StripeSyncBatch{config: ssbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ssbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ssbq *StripeSyncBatchQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ssbq.querySpec()
	_spec.Node.Columns = ssbq.ctx.Fields
	if len(ssbq.ctx.Fields) > 0 {
		_spec.Unique = ssbq.ctx.Unique != nil && *ssbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ssbq.driver, _spec)
}

func (ssbq *StripeSyncBatchQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(stripesyncbatch.Table, stripesyncbatch.Columns, sqlgraph.NewFieldSpec(stripesyncbatch.FieldID, field.TypeString))
	_spec.From = ssbq.sql
	if unique := ssbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ssbq.path != nil {
		_spec.Unique = true
	}
	if fields := ssbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, stripesyncbatch.FieldID)
		for i := range fields {
			if fields[i] != stripesyncbatch.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ssbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ssbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ssbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ssbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ssbq *StripeSyncBatchQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ssbq.driver.Dialect())
	t1 := builder.Table(stripesyncbatch.Table)
	columns := ssbq.ctx.Fields
	if len(columns) == 0 {
		columns = stripesyncbatch.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ssbq.sql != nil {
		selector = ssbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ssbq.ctx.Unique != nil && *ssbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ssbq.predicates {
		p(selector)
	}
	for _, p := range ssbq.order {
		p(selector)
	}
	if offset := ssbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ssbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StripeSyncBatchGroupBy is the group-by builder for StripeSyncBatch entities.
type StripeSyncBatchGroupBy struct {
	selector
	build *StripeSyncBatchQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ssbgb *StripeSyncBatchGroupBy) Aggregate(fns ...AggregateFunc) *StripeSyncBatchGroupBy {
	ssbgb.fns = append(ssbgb.fns, fns...)
	return ssbgb
}

// Scan applies the selector query and scans the result into the given value.
func (ssbgb *StripeSyncBatchGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ssbgb.build.ctx, ent.OpQueryGroupBy)
	if err := ssbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StripeSyncBatchQuery, *StripeSyncBatchGroupBy](ctx, ssbgb.build, ssbgb, ssbgb.build.inters, v)
}

func (ssbgb *StripeSyncBatchGroupBy) sqlScan(ctx context.Context, root *StripeSyncBatchQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ssbgb.fns))
	for _, fn := range ssbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ssbgb.flds)+len(ssbgb.fns))
		for _, f := range *ssbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ssbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ssbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StripeSyncBatchSelect is the builder for selecting fields of StripeSyncBatch entities.
type StripeSyncBatchSelect struct {
	*StripeSyncBatchQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ssbs *StripeSyncBatchSelect) Aggregate(fns ...AggregateFunc) *StripeSyncBatchSelect {
	ssbs.fns = append(ssbs.fns, fns...)
	return ssbs
}

// Scan applies the selector query and scans the result into the given value.
func (ssbs *StripeSyncBatchSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ssbs.ctx, ent.OpQuerySelect)
	if err := ssbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StripeSyncBatchQuery, *StripeSyncBatchSelect](ctx, ssbs.StripeSyncBatchQuery, ssbs, ssbs.inters, v)
}

func (ssbs *StripeSyncBatchSelect) sqlScan(ctx context.Context, root *StripeSyncBatchQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ssbs.fns))
	for _, fn := range ssbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ssbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ssbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
