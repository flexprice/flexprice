FLEXPRICE_API_KEY=sk_your_server_key
# Optional, defaults to api.cloud.flexprice.io
FLEXPRICE_API_HOST=api.cloud.flexprice.io
# Per-unit cost used by the demo summary if events don't include cost
UNIT_COST=0.02
