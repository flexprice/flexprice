# Claude Code session data
.claude/
# Local environment overrides (may contain real secrets)
values.local.yaml
values-prod.yaml
values-staging.yaml
values-dev.yaml
# Local values override (may contain dev secrets)
values-local.yaml
# Helm dependency tarballs — restored via `helm dependency update`
charts/
# Helm package output
*.tgz
# Rendered manifests (helm template output)
rendered/
manifests/
